- Option monad, use when reading theme value from local storage
- update rust examples
- Form validation
- Hot reload debounce
- RouteNotExist in lib_web::Error
- UnSupportedMediaType in lib_web::Error
- FormRejection in lib_web::Error
- JsonOrFormRejection in lib_web::Error

when registering throw correct error of confliction email

- logout route should only run logout logic if auth token is present else redirect to home

- svelte flip animation for toasts
  - When a new element is appended at the bottom of a container, Existing elements shift upward smoothly, instead of jumping instantly.
